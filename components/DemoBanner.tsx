export default function DemoBanner() {
  return (
    <div className="rounded-lg border border-dashed border-ink-faint bg-paper-alt px-3 py-2 text-xs text-ink-soft">
      Modo demonstração: Supabase não configurado (ou indisponível), exibindo dados de exemplo.
    </div>
  );
}
